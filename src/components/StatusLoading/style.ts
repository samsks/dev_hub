import styled from "styled-components";

export const S_StatusLoading = styled.div`
  width: 100vw;
  height: 100%;

  position: absolute;
  top: 0;
  left: 0;

  background-color: var(--grey-2-fade);
`;
